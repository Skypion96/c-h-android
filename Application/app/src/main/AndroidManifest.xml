<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.computer_horizon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Modif_Profil"></activity>

        <receiver android:name=".MyNotificationPublisher" />

        <activity android:name=".DisqueDViewAll" />
        <activity android:name=".DisqueDListView" />
        <activity android:name=".CarteGViewAll" />
        <activity android:name=".CarteGListView" />
        <activity android:name=".ProcesseurViewAll" />
        <activity android:name=".ProcesseurListView" />
        <activity android:name=".OrdinateurAll" />
        <activity android:name=".Ordinateur" />
        <activity android:name=".Connexion" />
        <activity android:name=".Profil_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>